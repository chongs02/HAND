import React, { Component } from 'react';

export class HashTag extends Component {
    render() {
        return (
            <div>
                해쉬태그 여기
                
            </div>
        );
    }
}

export default HashTag;
